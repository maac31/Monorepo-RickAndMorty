# ExploradorBaas (Frontend) - Angular

Frontend en Angular que consume exclusivamente el backend propio (proxy a `/api`).

## Requisitos
- Node 24 LTS
- NPM
- Backend corriendo en `http://localhost:5000` (ajustable en `proxy.conf.json`)

## Instalación
```bash
npm install
